# yamllint configuration ~ https://yamllint.readthedocs.io
extends: default

# The YAML test:suite submodule is a conformance corpus full of intentionally
# malformed fixtures — never lint it (also covers manual `yamllint .` runs).
# pnpm-lock.yaml and node_modules belong to the dev-only Markdown toolchain and
# are machine-generated, so they are excluded too.
ignore: |
  *.template.yaml
  generated/
  pnpm-lock.yaml
  node_modules/
  .stryker-tmp/

rules:
  # false comment formatting checks (like requiring a space after '#')
  comments:
    require-starting-space: true
    ignore-shebangs: true
    min-spaces-from-content: 2

  # Commented-out config blocks (e.g. the false post-commit job) aren't
  # indented like surrounding content; don't flag that.
  comments-indentation: false

  # Our hand-written configs (lefthook.yml, mise tasks) don't start with `---`.
  document-start:
    present: false

  empty-lines:
    max: 2
    max-start: 0
    max-end: 0

  # Allow longer lines than the 80-char default; keep it advisory, not fatal.
  line-length:
    max: 200
    level: warning

  indentation:
    spaces: 2
    ignore: |
      *.template.yaml
      generated/
      pnpm-lock.yaml
      node_modules/
      .stryker-tmp/

  # GitHub Actions workflows use `on:` as a mapping key, which YAML 1.1 reads as
  # a boolean. Don't flag mapping keys for truthiness (values are still checked).
  truthy:
    check-keys: false
